using System;
using System.Collections.Generic;
using System.Text;

namespace StarterGame
{
    //ItemType is an enum used to allow items to be used during different cirumstances and not during others. 
    public enum ItemType
    {
        BasicItem, HealthItem, KeyItem, BattleItem, Weapon
    }
}
